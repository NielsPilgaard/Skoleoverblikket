// Shared markdown tag allowlist for the weekplan / SFO free-text fields.
// The editor (MarkdownTextarea) offers nothing these tags cannot render.
export const MD_ALLOWED_TAGS: string[] = ['p', 'strong', 'em', 'ul', 'ol', 'li', 'br']
